#!/usr/bin/env bash

curl -s -X POST --header 'Content-Type: application/json' --header 'Accept: application/json' -d '{ "meta": { "name": "java", "description": "Java is a general-purpose computer programming language that is concurrent, class-based, object-oriented, and specifically designed to have as few implementation dependencies as possible.", "author": "mlehmann1", "dependencies": [] }, "snippet": "FROM openjdk:8" }' 'http://localhost:8080/api/features'
curl -s -X POST --header 'Content-Type: application/json' --header 'Accept: application/json' -d '{ "meta": { "name": "scala", "description": "Scala is a general-purpose programming language with full support for functional programming and strong static type system.", "author": "mlehmann1", "dependencies": ["java"] }, "snippet": "FROM hseeberger/scala-sbt" }' 'http://localhost:8080/api/features'
curl -s -X POST --header 'Content-Type: application/json' --header 'Accept: application/json' -d '{ "meta": { "name": "guava", "description": "Guava is a set of core libraries that includes new collection types (such as multimap and multiset), immutable collections, a graph library, functional types, an in-memory cache, and APIs/utilities for concurrency, I/O, hashing, primitives, reflection, string processing, and much more!", "author": "mlehmann1", "dependencies": [] }, "snippet": "FROM fabric8/connector-guava-eventbus" }' 'http://localhost:8080/api/features'
curl -s -X POST --header 'Content-Type: application/json' --header 'Accept: application/json' -d '{ "meta": { "name": "maven", "description": "Apache Maven is a software project management and comprehension tool.", "author": "mlehmann1", "dependencies": ["java"] }, "snippet": "FROM maven" }' 'http://localhost:8080/api/features'
